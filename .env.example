# Discord bot credentials
DISCORD_TOKEN=
DISCORD_GUILD_ID=
# DISCORD_CLIENT_ID — code does not read this. Kept here as a reminder
# in case you want to hand-build the OAuth2 invite URL in docs/INSTALL.md 단계 3.
# DISCORD_CLIENT_ID=

# Local paths / defaults
SESSION_DB_PATH=./router.sqlite3
CMUX_CMD=/Applications/cmux.app/Contents/Resources/bin/cmux
DEFAULT_WORK_DIR=/Users/me/IdeaProjects
# KIMI_CMD — currently unused by the bot (kimi-cli is launched inside the
# cmux surface, not by the bot directly). Leave commented out.
# KIMI_CMD=kimi

# kimi-cli model auth (one of these must work)
# MOONSHOT_API_KEY=sk-...
